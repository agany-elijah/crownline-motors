import { describe, it } from "vitest"

// Placeholder for Stage 37. The source it covers (search/filter logic)
// is still a stub, so these are declared as todo rather than left as an
// empty file that fails the run.
describe("vehicle filters", () => {
  it.todo("returns the full published set when no filters are applied")
  it.todo("combines make, model, year, price and mileage filters")
  it.todo("rejects out-of-range and non-numeric filter values")
  it.todo("excludes vehicles that are not PUBLISHED")
})
