generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Models (Vehicle, Customer, Quote, Order, OrderItem, Payment, Shipment, TrackingEvent)
// are added in Step 2 — Core Schema.
